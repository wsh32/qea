What is the effect of parameters $A$ and $k$ on the curve?

\begin{solution}
    $A$ determines if the curve has positive or negative values. $k$ determines if the curve exponentially grows or exponentially shrinks.
\end{solution}